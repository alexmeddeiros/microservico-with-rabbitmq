spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest


rabbitmq-queue-proposta-pendente: proposta-pendente.ms-notificacao
aws:
  accessKey: ${ACESS_KEY}
  secretKey: ${SECRET_KEY}
